import styled from '@emotion/native';

/**
 * Using colors from theme example
 */

export const ExampleContainer = styled.View`
  flex: 1;
  background-color: ${props => props.theme.colors.light};
`;
